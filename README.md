# AnimeCards — карточный бот по аниме ✅

**AnimeCards** — простой Telegram-бот для коллекционирования карточек по мотивам аниме. Игроки собирают коллекцию карточек с разной редкостью.

**Бот:** @animcrds_bot

---

## Функции 🎯

- Раздача карточек пользователям
- Хранение коллекций и статистики
- Просмотр профиля и инвентаря
- Поддержка баз данных SQLite и PostgreSQL

---

## Установка 🚀

1. Создайте `.env` файл:

```dotenv
BOT_TOKEN=123456789:ABCDefGhIJKlmNoPqRstUvWxYz
DB_URL=sqlite+aiosqlite:///./database.db
REDIS_URL=redis://localhost:6379/0
```

2. Установите зависимости:

```bash
pip install .
```

3. Запустите бота:

```bash
python main.py
```

---

## Важно о базе данных ⚠️

⚠️ **Внимание:** Бот может работать некорректно в SQLite из-за использования типов данных, которых нет в SQLite. Для стабильной работы рекомендуется использовать PostgreSQL.

---

## Разработка 💡

- Для разработки можно использовать SQLite
- Для продакшена **рекомендуется** PostgreSQL
- Не коммитьте `.env` в репозиторий
- Логи в `log.txt`
